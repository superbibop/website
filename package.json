{
  "name": "atlas",
  "version": "1.0.0",
  "private": true,
  "description": "Homework tracker with ManageBac sync, deadline notifications, and a question-explaining assistant.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node test/notify.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0"
  },
  "comment": "The dashboard itself has no dependencies and runs without npm install. The SDK is needed only by the assistant route in server.mjs; without it the assistant falls back to its offline explainer.",
  "engines": { "node": ">=18" }
}
